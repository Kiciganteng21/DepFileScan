
[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = ">=2.28.0"
flask = ">=2.2.0"
sqlalchemy = ">=1.4.0"
django = "==4.1.0"  # Pinned version for demo
fastapi = "*"
uvicorn = "*"

[dev-packages]
pytest = ">=7.0.0"
black = "*"
flake8 = "*"
mypy = ">=0.991"
pytest-cov = "*"

[requires]
python_version = "3.11"

[scripts]
test = "pytest"
lint = "flake8"
format = "black ."
